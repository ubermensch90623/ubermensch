# Mermaid 도식 패턴 (경제학)

GitHub, Notion, 대부분의 Markdown 뷰어에서 자동 렌더링.

## 1. 통화정책 전달경로 (가장 자주 쓰임)

```mermaid
flowchart LR
    CB[중앙은행<br/>기준금리 조정] --> MR[시중금리]
    MR --> I[투자]
    MR --> C[내구재 소비]
    MR --> ER[환율]
    I --> AD[총수요]
    C --> AD
    ER -->|수출 변화| AD
    AD --> Y[GDP]
    AD --> P[물가]
    AD --> E[고용]
```

## 2. 재정정책 vs 통화정책 비교

```mermaid
flowchart TB
    Shock[경기 침체] --> Choice{정책 선택}
    Choice -->|재정정책| F[정부지출 증가<br/>또는 감세]
    Choice -->|통화정책| M[기준금리 인하<br/>또는 양적완화]
    F --> AD1[총수요 직접 증가]
    M --> Rate[시중금리 하락]
    Rate --> AD2[투자·소비 간접 증가]
    AD1 --> Y[GDP 회복]
    AD2 --> Y
```

## 3. 경제순환 (가계-기업-정부)

```mermaid
flowchart LR
    H[가계] -->|노동·자본 공급| F[기업]
    F -->|임금·이자·이윤| H
    F -->|재화·서비스 공급| H
    H -->|소비지출| F
    H -->|세금| G[정부]
    F -->|세금| G
    G -->|이전지출·공공서비스| H
    G -->|보조금·공공조달| F
```

## 4. 시장구조 4분류

```mermaid
graph TB
    M[시장구조] --> PC[완전경쟁시장]
    M --> MO[독점적경쟁시장]
    M --> OL[과점시장]
    M --> MN[독점시장]

    PC -->|기업 수| PC1[다수 ∞]
    PC -->|상품| PC2[동질적]
    PC -->|진입장벽| PC3[없음]

    MO -->|기업 수| MO1[다수]
    MO -->|상품| MO2[차별화]
    MO -->|진입장벽| MO3[낮음]

    OL -->|기업 수| OL1[소수]
    OL -->|상품| OL2[동질 또는 차별화]
    OL -->|진입장벽| OL3[높음]

    MN -->|기업 수| MN1[1개]
    MN -->|상품| MN2[대체재 없음]
    MN -->|진입장벽| MN3[매우 높음]
```

## 5. GDP 측정 3접근

```mermaid
flowchart LR
    GDP((GDP)) --- P[생산접근<br/>부가가치 합]
    GDP --- E[지출접근<br/>C + I + G + NX]
    GDP --- I[소득접근<br/>임금 + 이자 + 이윤 + 임대료]
    P -.동일하다.- E
    E -.동일하다.- I
```

## 6. 인플레이션 원인 분류

```mermaid
flowchart TB
    Inf[인플레이션] --> Demand[수요견인]
    Inf --> Cost[비용인상]
    Inf --> Expect[기대인플레이션]

    Demand --> D1[통화량 증가]
    Demand --> D2[재정확장]
    Demand --> D3[수출 증가]

    Cost --> C1[원자재 가격]
    Cost --> C2[임금 상승]
    Cost --> C3[환율 절하]

    Expect --> E1[과거 인플레이션 학습]
    Expect --> E2[합리적 기대]
```

## 7. 게임이론 의사결정 트리

```mermaid
flowchart TB
    A1{기업 A 선택}
    A1 -->|진입| B1{기업 B 선택}
    A1 -->|진입 안 함| O1["(0, 5)"]
    B1 -->|수용| O2["(2, 2)"]
    B1 -->|가격전쟁| O3["(-1, -1)"]
```

각 노드의 보수는 `(A의 보수, B의 보수)` 형식.

## 8. 외부효과 — 사적·사회적 비용

```mermaid
flowchart LR
    Q[생산량 결정] --> PMC[사적 한계비용 PMC]
    Q --> SMC[사회적 한계비용 SMC]
    PMC --> QP[사적 균형 Q_p]
    SMC --> QS[사회적 최적 Q_s]
    QP -.|Q_p > Q_s| DWL[과잉생산<br/>사중손실]
    DWL --> Tax[피구세 / 배출권]
    Tax --> QS
```

## 사용 가이드

- 노드는 **한글 + 기호** 형식: `[총수요 AD]`, `[GDP Y]`.
- 화살표 라벨은 `-->|증가|`, `-->|하락|` 으로 방향성 명시.
- 인과 관계: `-->`. 동치/항등 관계: `-.동일하다.-`.
- 박스 모양으로 의미 구분:
  - `[직사각형]` — 변수/상태
  - `((원형))` — 합산/총량
  - `{마름모}` — 의사결정 분기
  - `[(파일)]` — 데이터/통계
