# P12 검토 — NCS 수리능력 전용 전환

> **판정: HYBRID (엔진 재사용 + 카드 소스 교체 + 문자열 리브랜딩)**
>
> 사유 (250자): 77bf3ef 의 drill_loader/server/UI 는 "해설카드 HTML 85 → ①②③④⑤ 5지선다 채점 + attempt/star/classification 저장" 구조로 **도메인 중립 회독 엔진**이다. 단, 현 85카드 중 수리 0개 확정이라 **콘텐츠 레이어는 전면 교체**. 엔진 RESET 은 D-21(캠코 5/9) 상황에서 C-0 북극성 위반 — 이미 동작하는 attempt/star/오답분류 코드를 버리는 건 복원률 95% 에 음의 기여. 단 서브토픽/도메인 tree, "해설카드 85" 하드코딩 문자열, `/study/` CARDS_DIR 은 수리 소스에 맞춰 리바인딩 필수.

---

## Q1. 기존 엔진 재사용 가능성 — **YES (조건부)**

### 재사용 가능 (도메인 중립)
- `server.py` /api/drill/items, /api/progress (attempt/star), 통계 집계 — **경제/수리 무관**
- `state.js` Alpine store: `selected/submitted/saveAttempt/classification('unknown'|'missed')/toggleStar/wrongIdSet/starredIdSet` — 5지선다 문제면 전부 동작
- `index.html` 3패널 레이아웃 + 다크모드 토큰 + 키바인딩 구조
- 오답 분류 "몰라서(unknown)/알았는데 못 알아챔(missed)" — 이건 **종환 학습 프로토콜 §8-2** 9번 "오답 분류: 몰라서 틀림 vs 알았는데 못 알아챔 분리" 와 정확히 일치. 버리면 손실

### 수리 전환 시 필수 수정
1. `drill_loader.py` L38 `CARDS_DIR = "...Cowork_작업폴더/해설_카드"` → 수리 소스 디렉토리로 재지정
2. `drill_loader.py` L48 CHOICE_P_RE 는 `<p>①…` 포맷 가정 — 수리 문항 HTML 템플릿이 다르면 파서 개별 작성
3. `drill_loader.py` L54-58 card0/story-box 셀렉터 — 수리 카드 DOM 에 맞춰야
4. `index.html` L6 title, L73 "해설카드 85" 하드 텍스트, state.js L1/L20 주석 — 리브랜딩
5. `/study/` 경로 서빙 폴더 (server.py L142) — 수리 HTML 루트로

**결론**: 엔진은 살리고, **파서·소스경로·표시문자열 3개 레이어**만 수리 전용으로 교체.

---

## Q2. 문항 소스 판결

### (α) 해커스 377 NotebookLM — **1순위 (조건부)**
- **장점**: 종환 **소유 22소스**, 이미 Gemini+NotebookLM 에 학습됨. 1차 자료 (구매 이북) = C-0 하드 기준 3/4 즉시 충족 (출처·인용·발행일)
- **리스크 (C-0 정면 충돌)**: ubermensch/CLAUDE.md §5 **"구매 이북 원문 저장소 커밋 금지"** — drill_items.json 에 문제·선지 전문 박으면 git 커밋 불가
- **해결책**: `~/.exam_corpus.json` (이미 §4.3 로 지정된 로컬 전용 경로) 에 저장 + drill_loader 가 로컬 파일 참조. 저장소에는 **ID·도메인·정답인덱스 메타만** 커밋 (저작권 없는 스키마)
- **판정**: GO. 단 최초 20-30 문항은 종환이 직접 OCR/타이핑 또는 Gemini 로 추출 → 로컬 저장. 종환 시간 소모 있음

### (β) NCS 공식 매뉴얼 PDF (ncs.go.kr) — **2순위 (보조)**
- **C-0 확정 팩트**: 공공누리 제2유형·공공기관 공식 발행. URL·원문·날짜 4/4 충족
- **리스크**: 실제 후기상 캠코·공기업 필기는 **해커스 스타일(계산·자료해석)** 에 가깝고 NCS 매뉴얼 예시문항은 **교수자용 개념도구**. 복원률 95% 기여도 낮음
- **판정**: 보조 자료로만. 기초개념·공식 유도 설명 카드 20개 내외

### (γ) Gemini NCS 수리튜터 Gem 대화이력 — **3순위 (재활용)**
- 종환이 이미 풀었던 문항 + Gemini 해설 누적. **종환 약점 데이터 풍부** (전사실수/직관우선 패턴)
- **C-0**: 대화이력은 1차 자료가 아님 (Gemini 생성물) — **격리 영역**
- **활용법**: 문항은 해커스 377(α) 에서 가져오되, **해설 스타일**을 Gemini 대화이력에서 학습. 종환 맞춤 해설 양식으로

### (δ) 세션 신규 문항 작성 — **REJECT**
- P9 리버스엔지니어링 실패 전례 (23/30 상상) + C-0 "입력 중 하나라도 격리면 출력 전부 격리"
- 수리는 **숫자 정확성**이 생명. 상상 문항은 종환 "글자꼼꼼/전사실수 약점" 직격으로 오답 스노우볼

**최종 소스 계층**: α (해커스 377) 60문항 → γ 해설 스타일 적용 → β 개념카드 보조

---

## Q3. 4하위능력 우선순위

NCS 수리능력 표준 4분류: 기초연산능력·기초통계능력·도표분석능력·도표작성능력

| 순위 | 능력 | 종환 배치 근거 |
|------|------|---------------|
| **1** | **도표분석능력** (자료해석) | 캠코 NCS 20문 中 전통적 비중 최고. 종환 약점 `user_jonghwan_weakness_pattern` 의 **전사실수+직관우선**이 가장 치명. 해커스 377 수록 비중 최대 추정 |
| **2** | **기초연산능력** (응용수리) | 속도·농도·일률·확률. `feedback_one_problem_at_a_time` + `feedback_no_latex` 직접 적용 영역. 공식 발동조건 약점 직격 |
| **3** | **기초통계능력** | 평균·분산·표준편차. 공기업 NCS 에선 도표분석과 통합 출제 경향 |
| **4** | **도표작성능력** | 객관식 필기에서 직접 출제 거의 없음. 스킵 가능 |

**근거 부족 경고 (C-0)**: "캠코 NCS 수리 4영역별 출제 비율 X:Y:Z:W" 는 **공식 후기 없이 단정 금지**. 위 표는 **공기업 NCS 일반 경향 + 해커스 교재 목차 추정**. P3/P5 후기 스캔 후 재조정 필요 — `required_additional_materials`: 캠코 2024/2025 NCS 수리 파트 후기 5건

---

## Q4. 실행 타임라인

### 🔥 다음 30분 (즉시)
1. `drill_loader.py` 사본 `drill_loader_math.py` 생성 — CARDS_DIR 변수만 `harness/data/math_cards/` 로 치환 (원본 경제 엔진은 보존)
2. `harness/data/math_cards/` 폴더 생성 + 샘플 1개 HTML 수기 작성 (해커스 377 Q1 1문항, 손으로 타이핑, 로컬 전용)
3. 서버에 `/api/drill/math_items` 라우트 추가 (경제 /api/drill/items 와 병행)
4. 종환에게 5분 내 확인: **"해커스 377 종이책 or PDF? OCR 가능한가?"** (소스 수령 경로 확정)

### 🎯 다음 2시간
5. 해커스 377 도표분석 1챕터 (약 15-20문항) 종환이 이미지 캡처 or 타이핑 → 로컬 `.exam_corpus_math.json`
6. drill_loader_math 파서가 해당 JSON 을 읽어 drill_items_math.json 생성 (HTML 템플릿 아닌 JSON 직접 입력 경로)
7. UI 에 "수리 전용" 탭 또는 루트 분기 — state.js 에서 `/api/drill/math_items` 로 fetch

### 📅 오늘 중 (D-20)
8. 도표분석 30문항 적재 → 종환 첫 드릴 세션 30분 (시험장 5대 룰 中 "1분 못 풀면 O치고 넘기기" UI 강제: 60초 타이머 표시)
9. 오답 15개 이상 나오면 종환 약점 패턴 태깅 (전사실수/직관/발동조건) — classification 필드 확장

### 🏁 D-21 → D-Day (5/9)
10. D-20 ~ D-7: 해커스 377 수리 전문항 (추정 250-300제) 적재 + 1회독
11. D-7 ~ D-2: 별 3개(★★★) 문항 집중 2-3회독. 시험장 5룰 시뮬레이션 (60분 60문 전체훑기)
12. D-1: 전사실수 체크리스트 + "시험 5분 전 불안 메모" 템플릿 UI 노출

---

## P12 글자단위 감찰 — 5 Red-Flag

1. **`index.html:6`** `<title>NCS Drill · 해설카드 85</title>` — "해설카드 85" 는 경제 콘텐츠 이름. 수리 전환 시 `NCS 수리 드릴` 로 변경
2. **`index.html:73`** `해설카드 <span x-text="items.length">` — 동일. "문항" 또는 "수리 문항" 으로
3. **`state.js:1`** 주석 `해설카드 85 직접 풀이 엔진` — 엔진이 경제 전용이라는 인상. `NCS 문항 회독 엔진` 으로
4. **`drill_loader.py:38`** `CARDS_DIR = "...Cowork_작업폴더/해설_카드"` — **하드코딩 경로**. 수리 분기 시 환경변수 또는 파라미터화 필수 (지금 수정 안 하면 경제/수리 동시 운용 불가)
5. **`components/progress_map.html:30`** `data-cell="경제학(격리)"` 셀 — 수리 진도 표시 UI 에 경제학 레이블 그대로 노출 시 종환 "글자꼼꼼" 약점 직격

---

## 자기 의심 — 이 판결의 약점 3개

1. **소스 α (해커스 377) 획득 경로 미검증**: 종환이 종이책인지 PDF 인지 Gemini 에 업로드된 원본이 있는지 모른 채 "GO" 판정. 실제로 OCR/타이핑 부담이 2시간 넘어가면 종환 "패턴 B 생각 과부하 → 멈춤" 발동. **즉시 확인 필요**
2. **캠코 NCS 수리 비중 추정이 C-0 미달**: Q3 의 1~4순위는 "공기업 일반 경향 + 교재 목차" 근거. 2024-2025 캠코 실제 후기 스캔 없이 단정 — 스노우볼 위험. 격리 플래그 필요
3. **P9 리버스엔지니어링 전례를 경제에 한정해 일반화**: 수리 신규 문항 작성 REJECT 판정은 맞지만, "해커스 377 문항을 소스로 해서 P9 가 **변형 문항**을 생성" 하는 경로는 검토하지 않음. 복원률 95% 최대화에 기여 가능성 있음 (다음 라운드 검토 안건)
