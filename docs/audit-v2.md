# 재감사 v2 — 환경 제약 판명 후 이전 작업물 재평가

> 목적: audit-v1 작성 시점엔 WebFetch 제약이 밝혀지지 않았음.
> 이제 한국 채용/커뮤니티 도메인 대부분 **403** 으로 확인된 상태에서,
> 이전 작업물이 여전히 유효한지 재검토.
>
> 원칙 (CLAUDE.md 상속): 의심 우선 · 팩트체크 · 출처 · 데이터 혈통.

## 1. audit-v1.md 재평가

### 여전히 유효한 지적 ✓
- analyzer.py `_feasibility_of_raw_delta` 의 임의 임계값 ← R1 에서 제거됨 (b8ef43e)
- `_recommend_scenario` 의 "약점 집중=옳다" 휴리스틱 ← R1 에서 중립 trade-off 로 교체됨
- `_build_checklist` "모의고사 5회" ← 제거됨
- i18n 휴리스틱 레이블 ← 제거됨
- 테스트의 휴리스틱 강제 ← 수학만 남김

### v1 에서 **새로 추가해야 할 지적**
- v1 §3 의 "나는 응답에 '휴노/ORP/KHBR 실존' 이라 썼다" — 실존은 맞으나 **KINFA 수의계약 여부 미확인** 은 그대로. 업데이트: "6대 출제사 중 누구와 수의계약했는지 여전히 미확인 (R4 에서 확인 대기)".
- v1 §4 "Plan-mode 오류" 원인 분석 (가설 2+3) — **2차 발생 확인됨**. 원인 보완: 서브에이전트는 자체적으로 plan mode 에 진입하려는 기본 성향 있음. CLAUDE.md §2.2a 우회 템플릿 제정됨. 아직 실전 검증 미완.

## 2. docs/reconstruction-plan.md (v3) 재평가

### 유효한 부분 ✓
- 소스 계층 (1차>3차>2차>>비소스) — 원칙 견고
- 자동 루틴 R1~R12 — 큰 틀 유효
- 에이전트 편제 (P1~P9 + SC1/SC2) — 설계 유효

### 수정 필요 🔧
- **R3 가정 오류**: plan v3 은 "Agent P1 이 공식 공고 fetch 가능" 가정. 실제로는 fetch 제약.
  - 수정: "P1 = 주로 WebSearch 스니펫 마이닝 + 사용자 제공 공고 PDF 분석" 으로 역할 재정의
- **R4~R6 도 동일 리스크**: 나라장터·알라딘·후기 사이트 모두 403 가능성. 각 Phase 에 "fetch 실패 시 대안" 명시 필요
- **에이전트 파견 가정 재검토**: 서브에이전트가 plan-mode 에 빠지는 경향 확인. **주요 웹 조사는 메인 세션에서 수행** 이 현재 기본 전략. 서브에이전트는 분석·합성 (fetch 결과가 이미 텍스트로 있을 때) 에 활용.

## 3. CLAUDE.md 정합성 점검

### 현재까지 제정된 조항 목록
- §북극성: 복원률 95%
- §헌법 C-1: 에이전트 10년+ 박사급 (개정 금지)
- §1 ZERO-TOLERANCE + 7 세부규칙 + 8 교차심문 + 9 한계/교재요청
- §1 데이터 혈통 + 계획검토실행루프 + 자기업그레이드
- §2 에이전트 파견 프로토콜 (2.1~2.7a)
- §3 API Timeout 방지
- §4 세션 연속성 (Phase 0 블로커)
- §5 허용/금지 동작

### 정합성 OK ✓
- 헌법-북극성-원칙 간 충돌 없음
- 각 조항이 다른 조항을 약화시키지 않음

### 보강 검토 🔍
- §2 파견 프로토콜: "서브에이전트가 여전히 plan-mode 에 빠진다면 → 메인 세션 직접 수행으로 fallback" 이라는 fallback 경로 **현재 미기재**. 추가 권장.
- §3 Timeout 방지: "Write 150줄 이하" 규칙 있으나 audit-v1·reconstruction-plan 은 이를 초과했음. 다행히 분할 Edit 로 도달했으니 실제 timeout 재발 없음. 규칙 유효.

## 4. 코드베이스 재평가

### exam_analyzer 본체
- R1 이후 모든 테스트 통과 (39건).
- corpus 모듈 스캐폴딩 추가 (f7f1535). 기능 추가 아직 미완.

### 테스트 커버리지
- 수학 결정론적 로직: 충분
- corpus 모듈: smoke test 만, 정식 unittest 미작성 → **다음 단계에 추가 권장**
- CLI 통합: 유효

## 5. 데이터 혈통 체크

현재 저장된 사실 중 혈통 추적 가능한 것:
- 사용자 실점수 (58.09/64.48/-6.39): 사용자 이미지 → 신뢰
- NCS 3영역 등: WebSearch 요약 → 원본 URL 확인 불가 → **MID**, 공식 공고로 검증 필요 (M1 대기)

> 혈통 없는 주장이 코드·문서에 흘러들어가지 않았는지 전수 확인 결과: OK.
> 단, `docs/exam_reconstruction/.../10_snippets_low_confidence.md` 의 MID 항목은
> 최종 산출물 (40_reconstructed.md, 50_reverse_engineered.md) 로 **자동 승격 금지**.
> 반드시 공식 공고 원문 확인 후 수석위원 2명 승인 거쳐 이관.

## 6. 권고 조치 (이 audit 의 즉각 실행 항목)

- [ ] CLAUDE.md §2 에 "서브에이전트 plan-mode fallback: 메인 세션 직접 수행" 기재 (다음 Edit)
- [ ] corpus 모듈 정식 unittest 추가 (대기 시간 활용 가능)
- [ ] plan v3 의 R3 설명 문구 "fetch 가능 가정" → "fetch 실패 가능성·대안 명시" 로 보강 (다음 Edit)
- [ ] 필수자료 M1/M2/M3 사용자 응답 수령 시 즉시 재검증 루프 가동

## 7. 자가 감사 (audit 자체의 검증)
- 본 audit 의 주장 중 출처 불명: 0건 (모두 코드/문서 파일 내부 참조)
- 휴리스틱 단정: 0건 (수정 권고는 전부 "고려할 것" 수준)
- 데이터 혈통: OK
