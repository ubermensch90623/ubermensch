# Generator-verifier

Use when a second agent can check something the generator cannot reliably
self-evaluate (factuality, tests pass, schema compliance).

Run:
```bash
python -m examples.generator_verifier "prompt caching"
```
